Javascript development -- 

For connecting css file with HTML file -- <link rel="stylesheet" href="filename.css">
For connecting js file with HTML file -- <script src="filename.js></script>"

For getting random paragraph in html write lorem and press tab.
* For changing the text content of any html text element use give an id=" " to the element. Then --
document.getElementById("id").textContent = 'Hello';
-- The elemt then show the string stored in it like this.

let x; // declaration
x = 123; // assignment 
console.log(x)
console.log(`You are ${x} years old`)
console.log(typeof x)

let food = "Burger" // Strings
console.log(`Your favourite food is ${food}`)
let flag = false // Boolean

Operators  --> + - * / % etc. ** -- gives us the power of the digit. 2**3 gives us 8.
++ , -- , 
Precedence = Parenthesis --> ** (exponents) --> * / % --> + - -- Highest on the left side. 
IF operators of similar precedence given then solve then from the left side.

Taking UserInput --> Default for user input is String from the window promt.
let username = window.prompt("Enter the username?");

* or create a html button and take input from there.
HTML part -->
     <h1 id="UserH1"></h1>
    <label>Username</label>
    <input id="UserInput"><br><br>
    <button id="UserSubmit">Submit</button>
JS part -->
	document.getElementById("UserSubmit").onclick = function(){
		username =  document.getElementById("UserInput").value;
		document.getElementById("UserH1").textContent = `Hello ${username}`;
	}

Typeconversion in JS -->
x = String(x);
y = Number(y);
z = Boolean(z); // Gives true if theres any value given
--> String can be converted to boolean to check if its empty or not. If string is empty then on converting to boolean it returns false else itreturns true. 

On trying to type convert anundefined variable to number -- returns NaN.
On trying to type convert anundefined variable to boolean -- returns false.
On trying to type convert anundefined variable to string -- returns undefined.

CONST --> making a variable const makes sure that  it cannot be changed after that or js gives us a error.

MATH in JS --  its a built in object  in js providing diffn collection of math related properties and methods.
console.log(Math.E) or console.log(Math.PI)

Math.round() , .floor() , .ceil() , .trunc() , pow(x,y) , sqrt() , log() , sin() , cos() , tan() , abs() -- gives the absolute value ,
sign(x) -- if x positive -- ans = positive , if x negative -- and = negative , if x -- 0  then ans = 0  
Math.max(x,y,z) -- gives the max value. Math.min() -- min value.

Random no in JS --
let randno = Math.random() * 6; // gives random decimal nos. between 0 and 5.
console.log(randno)

Math.floor(math.random()) -- gives integer values.
Math.floor(Math.random() * 100) + 1 -- gives nos between 1 and 100.

Within range -->
const min = 50;
const max = 100;
let random = Math.floor(Math.random() * (max-min)) + min;
console.log(random);

* We can make designing easier by changing the formatting of all the classes.

IF - else if - else statement -- syntax like java. 
Executes a collection of codes if the consition is true else executes a different collection of codes.
-- use of && (and) and || (or) is also there.

TERNARY OPERATOR --
condition ? true statement : false statement;

Switch case -- same syntax as java. -- Alternate for  if-else

String methods in JS --
let st = "Hello" -- 
st.charAt( ) ; 
.indexOf( ) -- index of first occurence of the letter
.lastIndexOf( ) -- last index of
.length -- not method -- returns length of string
.trim() --- removes whitespaces after or before the string
.toLowercase()    .toUppercase()    .repeat(3) -- repeat the same string 3 times.
.startsWith(`H`) -- returns boolean , .endsWith() , .includes()
.replaceAll("a" , "b") -- replaces all a's with b's
.padstart(a , "0") -- adds padding to the string -- a = desired length of the string. and 0 is the character used to pad the string.
.padEnd(a , "0") -- adds padding to the end of the string.

** Using `print()` in JS -- prints the web page.

STRING SPLICING --
string.slice(0.4); -- starting index , ending index 
string.slice(-1) -- starts at the ending index. and on decreasing the index further it moves towards the back.

string.slice(0 , string.indexOf("A")); -- ending index is the first index where the character A is found.

Method chaining --
Calling one mathod after naother in one continuous line of code.
let username = "  roNItdeY  ";
username = username.trim().charAt(0).toUpperCase() + username.trim().slice(username.charAt(0)+1).toLowerCase();
console.log(username);
-- easier but can be complex when too long.

Logical operators -- AND ( && ) , OR( | | ) , NOT( ! );
== -- compares if values are equal.
=== -- strict equality -- compares if values and datatypes are equal.
!= -- inequality operator
!== -- strict inequality operator

LOOPS --
while(){ } , do{ } , while() , -- Use do-while loop when trying to run the column of code atleast once.
for(let i=0 ; i<10 ; i++)  -- continue -- skips that iteration of the loop and starts again from the next iteration.

** window.alert( " " ) -- used for a alert message.
** variable.isNaN -- used to check if its a number or not.

Functions --

function function_name( attribute1 , attribute2 ){  } 
-- calling a fucntion -- function_name( )

Eg -- loggin_info(username , password) { }

Variable scope in JS -- where variable is recognized and accessible.
local and global  -- functions cannot see inside other fucntions in JS.

# To use degree symbol in windows -- use ALT + 0176 on numpad.

ARRAYS in JS --
let fruits = ["apple" , "orange" , "coconut"];
fruits.push("walnut"); -- pushes new element to back of the array
fruits.pop( ); -- removes last element
fruits.unshift("mango"); -- adds element to the top the arrray.
fruits.shift( ); -- removes element from the top of the array.

fruits.length -- gives length of the array.
fruits.indexOf("apple") -- gives 0 as index of apple in array. If element doesnt exist then it returns -1.

for(let i=0 ; i<fruits.length ; i++)
{
	console.log(fruits[i]);
}
for(let fruit of fruits) -- advanced for loop.
{
	console.log(fruit);
}
fruits.sort( ) -- sorts the array. -- ascending
friuits.sort( ).reverse( ) -- sorts the array in reverse order. -- descending.

SPREAD Operator -- ( ... )
allows an iterable such as an array or string to be expanded into seperate elements.

let numbers = [ 1 , 2 , 3 , 4 , 5 ];
let max = Math.max(numbers) -- wrong as it cannot iterate to find the max no.
so we use the spread operator -- max = Math.max( ...numbers );

for strings too it can be used -- let name = "Ronit";
let letters = [...name]; -- unpacks the string into seperate elements.
console.log(letters);

We can join them again by using the join method --
letters = [...name].join("-");